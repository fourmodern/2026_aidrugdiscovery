# 항체 설계 하네스 — 의존성
# 무-날조 원칙: 아래 미설치 시 각 스크립트는 수치를 만들지 않고 graceful 폴백한다.

# --- 평가 파이프라인 (필수, GPU 불필요) ---
requests        # UniProt / RCSB PDB REST (antigen_lookup, antibody_search, humanness)
biopython       # ProtParam 물성 (developability), PairwiseAligner germline identity
                # (humanness), PDBParser+Superimposer Cα RMSD (design_rfantibody)

# --- 정식 항체 번호매김 (옵션, 강력 권장) ---
# 없으면 cdr_analysis.py 는 문서화된 휴리스틱 근사로 폴백하고
# method 필드에 "heuristic (approximate)" 를 남긴다. IMGT 정확 번호가 필요하면 설치할 것.
# abnumber       # ANARCI 래퍼. conda 권장: conda install -c bioconda abnumber
# anarci         # 원본. conda install -c bioconda anarci

# --- 설계 경로 A: ESMFold2 inversion (GPU 필요, RunPod) ---
# 공식 프로토콜 cookbook/tutorials/binder_design.py 의 헤더가 명시한 의존성:
# esm@git+https://github.com/Biohub/esm.git@main
# abnumber
# modal          # Modal 원격 실행용. 로컬/RunPod 직접 실행 시 --local 경로에서는 불필요하나
#                # 공식 스크립트가 import 하므로 설치되어 있어야 한다.
# torch          # 컨테이너 이미지에 포함(runpod/pytorch:...). GPU 점검에도 사용.

# --- 설계 경로 B: RFantibody (GPU 필요, RunPod) ---
# pip 패키지가 아님. https://github.com/RosettaCommons/RFantibody 를 clone 후
#   bash include/download_weights.sh && uv sync
# 또는 Docker/Apptainer 이미지 사용. NVIDIA GPU + CUDA 11.8+ 필요.
# 제공 CLI: rfdiffusion / proteinmpnn / rf2 / qvscorefile / qvls / qvextract ...
